diff --git a/media/gpu/v4l2/v4l2_queue.cc b/media/gpu/v4l2/v4l2_queue.cc
index 2e93c0bd8beb4..ff9bda8717753 100644
--- a/media/gpu/v4l2/v4l2_queue.cc
+++ b/media/gpu/v4l2/v4l2_queue.cc
@@ -750,7 +750,7 @@ bool V4L2WritableBufferRef::QueueDMABuf(scoped_refptr<FrameResource> frame,
 
   size_t num_planes = self.PlanesCount();
   for (size_t i = 0; i < num_planes; i++) {
-    self.buffer_data_->v4l2_buffer_.m.planes[i].m.fd = planes[i].fd.get();
+    self.buffer_data_->v4l2_buffer_.m.planes[i].m.fd = frame->GetDmabufFd(i);
   }
 
   return std::move(self).DoQueue(request_ref, std::move(frame));
